# Evidence Service v3.5.2.1

- Fixes robots.txt and llms.txt detection when the configured domain differs from the crawled/sitemap subdomain.
- AI hygiene now checks the primary domain, discovered sitemap domains and crawled owned page domains.
- Preserves checked URL attempts for hygiene debugging.
